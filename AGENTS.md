# 프로젝트 가이드라인 및 에이전트 지침 (AGENTS.md)

본 문서는 Next.js, React Konva, GSAP 기반의 'GONGWON' 프로젝트를 위한 에이전트 절대 행동 강령(Constitution)입니다. 에이전트는 코드를 작성하고 문제를 해결할 때 아래의 모든 철학과 원칙을 엄격하게 준수해야 합니다.

## 1. 작업자 컨텍스트 및 프로젝트 목표
- **작업자:** 대학교 인공지능학과 2학년 학생이며, **아트&테크놀로지학과 복수전공을 위한 포트폴리오**를 제작하고 있습니다.
- **포트폴리오 형태:** 거대한 단일 작품 하나를 보여주는 것이 아니라, 여러 개의 작은 작업물들(다작)을 효과적으로 담아내는 것이 주 목적입니다.
- **작업물 명칭:** 메인 페이지는 '#0 GONGWON'이며, 이후 오름차순 번호로 '#N 작업물명'과 같은 명칭이 붙습니다. 작업자는 #0 혹은 GONGWON 등으로, 번호 및 작업물명을 통해 어떤 디렉토리에서 작업해야 하는지 명령할 것입니다. 명령에 번호 및 작업물명이 포함되지 않은 경우 스스로 유추하십시오.

## 2. 핵심 디자인 철학 (Design Philosophy)
어떠한 기능 구현이나 리팩토링도 아래의 디자인 철학을 훼손해서는 안 됩니다.

1. **디지털과 아날로그의 완벽한 조화:** 철저하게 계산된 웹(디지털) 환경 위에, 거칠고 불규칙한 아날로그의 물리적 질감을 정교하게 렌더링합니다. 다양한 물리적 메타포를 적극 활용하여 모든 UI와 상호작용에 유기적인 아날로그 감성을 불어넣으십시오.

2. **아날로그 감성과 사용자 경험(UX)의 양립 (절대 규칙):** 시각적으로 아날로그의 거친 느낌(불규칙성, 노이즈, 번짐 효과 등)을 강하게 추구하더라도, **이것이 웹으로서의 쾌적한 사용성을 결코 해쳐서는 안 됩니다.** - 캔버스 화면 이동(Pan & Zoom)은 프레임 드랍 없이 극도로 부드러워야 합니다.
   - 클릭, 호버, 탭 전환 등의 상호작용은 딜레이 없이 직관적이고 명확하게 반응해야 합니다.
   - '아날로그적인 감성'이 사용자에게 '답답함'이나 '성능 저하'로 느껴지지 않도록, 심미성과 편의성 사이의 완벽한 기술적 균형을 유지하십시오.

---

## 3. 워크플로우 오케스트레이션 (Workflow Orchestration)
에이전트는 작업을 수행할 때 아래의 절차를 기본 상태(Default)로 삼습니다.

1. **플랜 모드 우선 (Plan Node Default)**
   - 3단계 이상의 절차나 아키텍처 결정이 필요한 모든 복잡한 작업은 반드시 플랜(계획) 모드로 진입하여 마크다운 형태로 사용자에게 먼저 제시하십시오.
   - 작업 진행 중 문제가 발생하면 무리하게 밀어붙이지 말고 즉시 멈춘 뒤 계획을 재수립(Re-plan) 하십시오.
   - 모호함을 줄이기 위해 코드를 짜기 전 상세한 명세(Specs)를 먼저 작성하십시오.
   - 모든 명세는 한국어로 작성하십시오.

2. **서브에이전트 전략 (Subagent Strategy)**
   - 메인 에이전트의 컨텍스트 창을 깔끔하게 유지하기 위해 서브에이전트(보조 AI)를 적극적으로 활용하십시오.
   - 복잡한 문제, 리서치, 병렬 분석 작업은 서브에이전트에게 위임하여 더 많은 연산 리소스를 투입하십시오.

3. **자기 개선 루프 (Self-Improvement Loop)**
   - 사용자로부터 수정 지시나 피드백을 받은 후에는 반드시 `lessons.md` (또는 프로젝트 내 지정된 로컬 메모리)에 해당 패턴과 교훈을 업데이트하십시오.
   - 동일한 실수를 반복하지 않도록 스스로를 위한 규칙을 작성하고, 작업 세션 시작 시 이를 복습하십시오.

4. **우아함 추구 (Demand Elegance)**
   - 복잡한 변경 사항을 다룰 때는 잠시 멈추고 "더 우아한 방법은 없는가?" 고민하십시오.
   - 작성한 코드가 임시방편(Hacky)처럼 느껴진다면, 현재 아는 모든 지식을 동원해 가장 우아한 해결책으로 다시 구현하십시오. (단, 단순한 오타 수정 등에는 과도한 엔지니어링을 피할 것)

5. **자율적 버그 수정 (Autonomous Bug Fixing)**
   - 사용자로부터 버그 리포트를 받으면, 어떻게 고칠지 묻지 말고 즉시 에러 로그와 실패 원인을 추적하여 스스로 해결하십시오.
   - 사용자의 컨텍스트 전환(Context switching)을 요구하지 않도록, 알아서 원인을 찾고 코드를 수정해 증명하십시오.

---

## 4. 작업 관리 (Task Management)
- **문서화:** 실행 계획은 `todo.md` 등에 체크 가능한 항목으로 작성하고, 작업 완료 시 이를 체크하며 진행률을 추적하십시오.
- **보고:** 각 작업 단계마다 무엇이 변경되었는지 큰 틀에서(High-level summary) 사용자에게 브리핑하십시오.

## 5. 핵심 원칙 (Core Principles)
- **단순함 우선 (Simplicity First):** 모든 변경 사항은 가장 단순하게 구현하며, 코드에 미치는 영향을 최소화하십시오. 기존의 상호작용과 UX를 망가뜨리는 부작용(Side-effect)을 경계하십시오.
- **나태함 금지 (No Laziness):** 문제의 근본 원인(Root cause)을 찾으십시오. 땜질식 임시 처방을 절대 금지하며, 시니어 개발자의 엄격한 기준을 유지하십시오.

## 6. 프로젝트 아키텍처 및 작업물 추가 지침 (Works Integration)
에이전트가 새로운 작업물(Work)을 포트폴리오에 추가할 때는 다음의 아키텍처 규칙을 엄격하게 준수해야 합니다.

1. **하위 프로젝트(Sub-route) 기반 개발**:
   - 모든 리액트/웹 기반 작업물은 단일 Next.js 프로젝트 내의 하위 경로로 구현합니다.
   - 경로 포맷은 `app/works/[work-id]/page.tsx` 형식을 따릅니다.
   - 메인 캔버스 맵과 작업물 페이지 간의 빌드/번들 꼬임을 막기 위해, Next.js의 코드 분할(Code Splitting) 이점을 활용하도록 설계해야 합니다.

2. **격리 및 캡슐화 (Encapsulation)**:
   - 각 작업물에서 개별적으로 사용되는 컴포넌트, 스타일시트, 에셋(이미지 등)은 `app/works/[work-id]/` 하위에 폴더 구조를 파서 격리시킵니다.
   - 전역 스타일(`app/globals.css`)을 임의로 수정하여 타 페이지에 부작용(Side-effect)을 일으키는 행동을 절대 금지합니다.

3. **공통 아날로그 테마 및 복귀 기능 연동**:
   - 모든 작업물 페이지는 공통 레이아웃인 `app/works/layout.tsx` 하위에서 렌더링되게 하여 통일된 아날로그 텍스처 배경과 크레용/종이 필터를 자동으로 적용받아야 합니다.
   - 사용자 경험(UX) 단절을 방지하기 위해, 각 작업물 화면 좌측 상단에는 메인 노드 맵으로 복귀하는 공통 컴포넌트(`app/works/components/BackToMapButton.tsx`)를 반드시 렌더링해야 합니다.

4. **하이브리드 링크 유연성 제공**:
   - Unity WebGL, Vanilla JS 등 타 빌드 환경의 특수 기술 스택 프로젝트의 경우에만 독립적인 외부 웹 프로젝트로 배포한 뒤 노드 데이터(`data.ts`)의 `linkUrl`에 아웃링크(`https://...`)를 기입합니다.
   - 포트폴리오 메인 대시보드(`Dashboard.tsx`)는 이 `linkUrl`의 형식을 감지하여 내부 라우트(Link 컴포넌트 이동)와 외부 사이트(a 태그 새 창 이동)를 유연하게 분기 처리하도록 유지해야 합니다.

5. **분산형 작업물 문서화 체계 (Decentralized Documentation System)** :
   - 문서 분리 원칙: 포트폴리오에 다수의 작업물이 추가됨에 따라 정보가 과부하되는 것을 막기 위해, 모든 정보를 하나의 문서에 몰아넣지 말고 '메인 코어 문서'와 '개별 작업물 문서'로 엄격히 분리하여 관리하십시오.
   - 메인 포트폴리오 문서 (예: docs/GONGWON.md): 여기에는 포트폴리오의 전체적인 아키텍처, 메인 캔버스(인터랙티브 노드 맵)의 핵심 동작 원리와 데이터 구조, 그리고 프로젝트 전체를 관통하는 핵심 디자인 의도(디지털과 아날로그의 조화)만을 상세히 기록하십시오. 메인 페이지와 전체 포트폴리오를 지칭하는 고유 라벨은 반드시 '#0 GONGWON'으로 표기합니다.
   - 개별 작업물 전용 문서 (예: docs/works/[work-id].md): 새로운 작업물이 추가될 때마다 docs/works/ 디렉토리 하위에 해당 작업물만을 위한 전용 마크다운 문서를 별도로 생성하십시오. 이 개별 문서에는 다음 항목이 반드시 포함되어야 합니다.
      - 공식 라벨링 및 프로젝트명: 작업물이 추가되는 순서에 따라 오름차순으로 번호를 부여하여 #N 프로젝트명 형식으로 기록 (예: #1 프로젝트명)
      - 작업물 고유 ID 및 프로젝트명
      - 라우트 경로 (app/works/...) 또는 외부 링크
      - 사용된 전용 핵심 기술 스택
      - 주요 기능 및 로직 명세
      - 직면했던 기술적 문제와 해결 과정 (Troubleshooting)
      - 해당 작업물 내에 적용된 아날로그 감성 반영 포인트
   - 선행 업데이트 원칙: 에이전트는 새로운 작업물을 기획 및 생성하거나 대규모 리팩토링을 진행할 경우, 실제 코드를 작성하기 전에 반드시 이 전용 문서들을 먼저 생성하거나 업데이트하여 명확한 컨텍스트와 라벨링 넘버를 유지해야 합니다.