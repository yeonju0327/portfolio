# Lessons Learned (배움 및 교훈 기록)

## 1. 리액트 클라이언트 마운트 지연과 Transient 세션 플래그의 레이스 컨디션
* **현상**: 마운트 시 `useEffect`에서 복원 플래그(예: `portfolio_should_restore`)를 삭제하면, 클라이언트 측 마운트 조건(`isClient && isReady` 등) 때문에 한 틱 늦게 최초 렌더링을 시도하는 하위 컴포넌트(`Sidebar` 등)들은 이미 삭제된 세션 값을 읽게 되어 상태 복원에 실패합니다.
* **해결책**: 하위 컴포넌트가 세션 스토리지를 직접 참조해 복원 여부를 감지하지 말고, 부모가 관리하는 지속성 있는 리액트 상태 `isRestored`를 prop으로 상속하게 함으로써 데이터 복원의 판단 기준을 단일화하고 렌더링 시점 지연 문제를 방지해야 합니다.

## 2. 부모 상태 해제 리렌더링에 따른 브라우저 애니메이션 재지정(Replay) 차단
* **현상**: 부모가 500ms 후 복원 상태 `isRestored`를 `false`로 갱신하여 자식을 리렌더링할 때, 자식 요소의 CSS 스타일 `animation` 속성이 `'none'`에서 다시 원래 애니메이션 명칭으로 롤백되면서 브라우저가 이를 새 애니메이션으로 인식해 **중복 실행(Replay)**하는 버그가 발생합니다.
* **해결책**:
  * 자식 컴포넌트가 마운트될 때 주입받은 `isRestored` prop을 `useState` 초기화 구문 내에서 로컬 상태 `initialIsRestored`로 복사해 굳힙니다.
  * 리렌더링 과정에서 부모 prop이 변경되더라도 고정 상태 `initialIsRestored`는 `true`가 유지되므로 스타일이 오염되거나 재조정되지 않아 애니메이션의 2차 중복 실행이 완벽히 방지됩니다.
  * 컴포넌트가 닫힌 뒤 나중에 노드를 클릭해 완전히 새로 마운트되는 대시보드는 복원 모드가 아닌 일반 상태(`isRestored = false`)로 마운트되므로 정상적인 열림 애니메이션이 안전하게 보장됩니다.

## 3. 미니맵 상의 미활성화 노드 시각적 간소화
* **현상**: 미니맵 내에 미활성화된 노드가 흐린 회색 등으로 노출될 경우, 포트폴리오 맵 내 진행 상태를 파악하는 직관성이 떨어질 수 있습니다.
* **해결책**: CSS `opacity` 및 배경색 처리 대신 미활성화 상태일 때는 `null`을 반환하여 렌더링에서 아예 배제(Conditional Rendering)합니다. 이를 통해 활성화된 상태의 메인 노드 맵에 대한 가시성과 집중도를 극대화할 수 있습니다.

## 4. SVG 자식 엘리먼트 스케일링 시 transform-origin 브라우저 호환성 및 오작동 문제
* **현상**: SVG `<circle>` 등의 자식 엘리먼트에 직접 `cx`, `cy` 값을 주고 GSAP의 `transformOrigin`을 동적으로 절대 픽셀 좌표(예: `cx px cy px`)로 적용하면, 브라우저가 원의 로컬 원점을 정확히 매칭하지 못해 스케일이 0으로 굳거나 다른 축을 기준으로 회전 및 왜곡되는 렌더링 누락 버그가 발생합니다.
* **해결책**:
  * 자식 엘리먼트들의 로컬 중심 좌표(`cx`, `cy`)를 전부 `0`으로 고정합니다.
  * 이들을 감싸는 부모 `<g>` 태그에 `transform="translate(cx, cy)"`를 주어 그룹 단위로 정확한 스크린 위치로 이동시킵니다.
  * 그 후 GSAP에서 `transformOrigin: "center center"`를 지정해 스케일링하면, 모든 브라우저에서 스크린 좌표의 원점을 기준으로 대칭적이고 안정적인 터널 확장/축소 애니메이션이 보장됩니다.

## 5. SVG <mask>는 동일 <svg> 내부 요소에만 적용됨 — 임시 노드 단일 SVG 통합 원칙
 
* **현상**: `TransitionOverlay`에서 페이지 전환 시 사용하는 임시 복제 노드(이미지 div + 테두리 SVG)가 SVG `<mask>`(도넛 마스크) 적용을 전혀 받지 못해, 이미지가 마스크 구멍 너머까지 삐져나오고 테두리 원이 비정상적으로 렌더링되는 버그가 발생했습니다.
* **원인**: SVG `<mask id="...">` 는 **선언된 `<svg>` 태그 내부의 요소**에만 적용됩니다. 이미지를 `<div>` 태그로, 테두리를 별도 `<svg>` 태그로 렌더링하면 마스크 참조(`mask="url(#...)"`)가 동작하지 않습니다.
* **해결책**: 전환 애니메이션에 필요한 모든 임시 요소(마스크 정의, 배경 rect, 이미지, 테두리 원)를 **단일 `<svg>` 안으로 통합**합니다.
  * 이미지는 `<div>` → SVG `<image>` 태그로 전환하고 `<clipPath>`로 원형 클리핑.
  * SVG 내 선언 순서(아래→위): `<image>` → 마스크 `<rect>` → 테두리 `<circle>`.
  * 이 구조에서 마스크 `<rect>`가 `<image>` 위를 덮어, 도넛 구멍(내부 검정 원 영역)에서만 이미지가 자연스럽게 노출됩니다. 구멍이 닫히면 `<rect>`가 이미지 전체를 덮어 이미지가 자동으로 사라집니다.

## 6. 페이지 이동 및 캔버스 복원 시 마운트 갭/라우터 상태 결합으로 인한 깜빡임 방지

* **현상**: 메인 지도 복귀 시 오버레이가 순간적으로 걷혔다 덮이거나, 상세 페이지 진입 시 라우팅 완료 찰나에 임시 노드가 홱 사라지는 등 1프레임 미만의 시각적 튐/깜빡임(Flicker) 현상이 있었습니다.
* **원인**:
  1. 복귀 시 복원 로직이 `useEffect` 내에서 비동기로 실행되면서 컴포넌트 마운트 직후 최초 렌더링 프레임 동안 transition 상태가 활성화되지 못했습니다.
  2. 상세 진입 시 트랜지션의 성격(뒤로 가기 여부)을 `pathname`에 의존하여 판별하고 있어, 라우팅으로 URL이 바뀌는 순간(트랜지션이 끝나기 전) 상태 판별이 오염되었습니다.
* **해결책**:
  1. SSR에 안전한 `useIsomorphicLayoutEffect`를 정의하고 적용하여 마운트 복원 `playInTransition`을 화면을 그리기(Paint) 전에 동기적으로 실행시켰습니다.
  2. `TransitionContext` 내부에 `isBackTransition`을 명시적 상태 변수로 분리하여 `pathname` 및 라우팅 상태 변화와의 결합도를 제거했습니다.

## 7. 상세 진입/복귀 트랜지션 중심점을 마지막 노드 좌표로 유지 및 세션 역산 복원

* **현상**: 상세 페이지 진입 완료(`in-detail` 오프너) 및 복귀 애니메이션(`startBackTransition` 클로저) 수행 시, 트랜지션 원형 마스크의 중심축이 화면 중앙으로 강제 초기화되어 노드의 원래 위치와 불일치하는 연출 단절이 있었습니다. 특히, 상세 페이지에서 브라우저 새로고침(F5)을 하면 React Context 상태가 휘발되어 중심 정보가 완전히 소실되었습니다.
* **원인**: 상세 진입 완료 및 복귀 콜백 함수 내부에서 `centerPos`와 `radius` 등을 화면 중앙값과 기본 상수로 무조건 덮어쓰고 있었습니다.
* **해결책**:
  1. 트랜지션 진입/복귀 시 기존에 세팅된 `centerPos`, `radius`, `imageRadius` 값을 그대로 계승하도록 강제 덮어쓰기 로직을 전면 제거했습니다.
  2. 새로고침으로 인해 Context가 비어있는 경우, `sessionStorage`에 백업된 지도 뷰포트 정보(`portfolio_viewport`) 및 마지막 포커스 노드 ID(`portfolio_focused_node`)를 조회하여, 상세 페이지 복귀 시점에도 메인 맵상의 스크린 좌표와 노드 스케일을 정확히 동적 역산해내는 복원 경로를 설계하여 적용했습니다.

## 8. State를 변경하는 useCallback 함수가 자기 자신을 의존성 배열에 품어 발생하는 무한 리렌더링 차단 (useRef State-Sync 패턴)

* **현상**: 상세 페이지 진입 후 트랜지션 걷히기 애니메이션이 무한 반복(루프)되는 치명적인 오작동이 일어났습니다.
* **원인**: `playInDetailTransition` 함수가 `centerPos` 등의 상태를 의존성 배열에 가지고 있으면서, 함수 내부에서 이 상태를 직접 수정하였습니다. 이로 인해 상태 변경 -> Provider 리렌더링 -> useCallback 참조값 변경 -> 하위 컴포넌트(`WorksLayout`)의 `useEffect` 재실행 -> `playInDetailTransition` 재호출이라는 무한 루프 피드백 고리가 발생했습니다.
* **해결책**:
  - `TransitionProvider` 내에 상태 트래킹용 `useRef`(`centerPosRef`, `radiusRef` 등)를 쌍으로 선언했습니다.
  - 상태 변경 및 ref 갱신을 동시에 보장하는 `setCenterPos` 등의 래퍼 함수를 `useCallback`으로 메모이징하여 참조를 불변 처리했습니다.
  - 트랜지션 함수 내부에서는 이 Ref의 최신 값을 조회하게 하고 의존성 배열을 `[]` 또는 `[router]` 수준으로 완전히 비워 함수 참조가 재생성되지 않도록 원천 스태빌라이징(Stabilize)하였습니다.

## 9. 다중 컴포넌트에 파편화된 핵심 지도 상수(VIRTUAL_SIZE) 동기화 의무

* **현상**: 배경 캔버스의 가상 탐색 범위를 넓히는 과정에서 메인 캔버스 컴포넌트 외에 미니맵 및 좌표 데이터 파일에도 동일한 크기의 상수가 개별로 하드코딩되어 동작하고 있었습니다.
* **원인**: 캔버스 크기를 나타내는 `VIRTUAL_SIZE`가 `Main.tsx`, `MiniMap.tsx`, `data.ts` 에 독립적인 상수로 선언되어 있었습니다. 한 곳만 수정할 경우 미니맵의 뷰포트 비율 계산 불일치 및 드래그 제약 조건(clamp) 계산 에러를 초래합니다.
* **해결책**:
  - `Main.tsx`, `MiniMap.tsx`, `data.ts` 세 파일에 흩어져 있던 `VIRTUAL_SIZE` 상수를 `8000`으로 일관되게 동시 확장하여 동기화했습니다.
  - 이로 인해 미니맵의 뷰포트 맵핑 및 집 복귀 기능(`CENTER = VIRTUAL_SIZE / 2`), 드래그 경계가 이상 없이 완전히 비례하여 확장되었습니다.

## 10. 초고해상도 Canvas 버퍼와 SVG 필터(feTurbulence) 중첩 연산 횟수(Octaves) 최적화를 통한 렉 현상 해소

* **현상**: 가상 캔버스 크기를 `8000px`로 확장한 이후 마우스 드래그 및 화면 줌인/줌아웃 시 마이크로 렉 및 극심한 프레임 드랍이 유발되었습니다.
* **원인**: HTML5 Canvas의 픽셀 해상도가 8000x8000(6천 4백만 픽셀)이 되면서 버퍼 용량이 한계를 넘었고, 여기에 중첩 루프 계산이 거대한 `#crayon-texture` (`numOctaves="3"`) 및 `#static-paper-edge` (`numOctaves="5"`) 필터가 캔버스 전체 영역에 대해 작동하여 CPU/GPU 병목이 일어났습니다.
* **해결책**:
  - `VIRTUAL_SIZE`를 기존 `4000px`보다 넓으면서 8000px 대비 픽셀 연산량을 약 61% 절감한 **`5000px`**로 현실적 타협 셋팅했습니다.
  - 크레용 질감 필터의 `numOctaves`를 3에서 **2**로, 찢어진 종이 필터의 `numOctaves`를 5에서 **3**으로 각각 하향 튜닝했습니다.
  - 시각적 완성도(아날로그 텍스처)는 육안상 차이 없이 고스란히 유지하면서, 드래그 및 줌인/줌아웃 연산 속도를 획기적으로 향상시켜 부드러운 60fps에 준하는 드래그 UX를 회복했습니다.

## 11. 대시보드 애니메이션 곡선 통일, 닫기 즉각 상호작용 및 사이드바 오프셋 조정

* **현상**:
  1. 대시보드 컴포넌트(`Dashboard.tsx`)의 GSAP 애니메이션과 `globals.css`의 CSS 애니메이션이 동일 요소의 `transform`/`opacity`를 중복 제어하여 스타일 충돌이 유발되었습니다.
  2. 대시보드가 닫힐 때 여는 애니메이션의 감속 곡선(`cubic-bezier(0.16, 1, 0.3, 1)`)과 대비하여 가속 성향이 다른 곡선이 적용되어 인터랙션의 통일성이 결여되었습니다.
  3. 대시보드가 닫히기 시작해서 완전히 사라지기까지 0.8초 동안 전체 화면 클릭 차단 마스크와 본체 요소가 마우스 이벤트를 가로막아 캔버스와의 즉각적인 상호작용이 불가능했습니다.
  4. 사이드바가 닫혔을 때(`left: -350px`) 본체 나무 테두리가 화면 왼쪽 벽에 노출되어, 아날로그 포스트잇만 벽에 이쁘게 붙어있는 메타포의 완성도를 해쳤습니다.
* **해결책**:
  1. **중복 스타일 제어 제거**: `Dashboard.tsx`에서 GSAP의 스타일 가변 연산 코드를 완전 제거하고, 순수 리액트 `setTimeout` 기반 타이머로 `animState` 생명주기만 제어하게 단순화하여 CSS 애니메이션과의 충돌을 해소했습니다.
  2. **감속 곡선 통일**: 여는 애니메이션과 닫는 애니메이션 모두 동일한 **`cubic-bezier(0.16, 1, 0.3, 1)`** 감속 곡선(초반 고속 진행 후 후반 완만 감속) 및 **`0.8초`**의 시간으로 통일하여, 열기와 닫기가 완벽한 대칭적 감성을 지니도록 조정했습니다. (모바일 닫기 `memoSlideDown`에도 적용)
  3. **닫기 개시 시 즉각 상호작용**: `animState === 'leaving'`이 되는 즉시 전체 화면 클릭 마스크를 언마운트하고, 대시보드 판넬과 내부 컨테이너의 `pointer-events` 속성을 `none`으로 동적 변환하여 대시보드가 사라지는 중에도 사용자가 캔버스를 즉각 클릭/드래그할 수 있게 개선했습니다.
  4. **사이드바 오프셋**: 사이드바가 닫혔을 때 위치 좌표를 `-350px`에서 **`-365px`**로 밀어 넣어 본체 나무 테두리는 완전히 숨기고, 우측의 INDEX/MAP 포스트잇만 정갈하게 노출시켰습니다.

## 12. React 19 / Next.js 16 (Turbopack) StrictMode 이중 마운트 애니메이션 무한 리렌더 및 크래시 제어
* **현상**: Next.js 16 Dev 서버 구동 중 특정 상세 페이지 레이아웃(`app/works/layout.tsx`) 내 진입 애니메이션이 이중 마운트(`StrictMode`)에 의해 중복 실행되어 렌더링 루프 및 Next.js Turbopack 컴파일러의 메모리 누수/크래시(Panic)를 유발했습니다.
* **원인**: React 19의 개발 모드에서는 컴포넌트 마운트/언마운트 사이클을 강제로 두 번 수행하는데, 애니메이션 시작 함수가 이중 호출되면서 싱글톤이 아닌 GSAP 인스턴스 또는 마운트 완료 플래그가 꼬여 무한 루프 상태에 빠졌습니다.
* **해결책**: `layout.tsx` 내에 `useRef(false)`로 `hasPlayedRef` 플래그를 생성하고, 애니메이션 개시 직전 `if (hasPlayedRef.current) return; hasPlayedRef.current = true;` 구문을 걸어 두 번 마운트되더라도 최초 1회만 트랜지션 로직이 실행되도록 가드(Guard)를 설계하여 해결했습니다.

## 13. React 19 인라인 style 내 의사 클래스(:active 등) 중첩 형태 작성 금지
* **현상**: JSX 렌더링 시 인라인 `style` 객체 안에 `:active` 나 `:hover` 와 같은 의사 클래스 스타일을 중첩 객체 형태로 작성했을 때 Next.js Turbopack 런타임에서 파서 크래시 및 컴파일 에러를 일으켰습니다.
* **원인**: React 19부터는 스타일 바인딩 파싱이 더 엄격해졌으며, 인라인 `style` 속성은 평탄한 CSSProperty 선언만 유효합니다. 의사 클래스(CSS Pseudo-class)는 인라인에 직접 객체로 중첩 주입할 수 없습니다.
* **해결책**: 중첩 스타일을 평탄화된 일반 인라인 스타일로 정정하거나, 복잡한 상호작용 스타일은 TailwindCSS나 별도 CSS 파일, 또는 JS 상태 기반 삼항 연산자로 평탄한 style 값을 동적으로 반환하여 해결했습니다.

## 14. 3D 투명 유리의 WebGL Depth Sorting (투명도 순서 정렬) 해결법
* **현상**: 3D Scene 내에서 여러 개의 투명한 유리 카드 메쉬가 앞뒤로 겹치며 다량 적재될 때, 뒤에 있는 카드가 앞의 카드를 뚫고 렌더링되거나 깜빡이는 WebGL 소팅 깨짐 문제가 발생했습니다.
* **해결책**:
  * 유리의 광학 셰이더인 `MeshPhysicalMaterial` 옵션에서 `depthWrite: false`를 지정하여 깊이 버퍼 쓰기를 차단했습니다.
  * 카드가 분배된 순서(`card.dealOrder`)에 맞추어 카드 메쉬들의 `renderOrder`를 오름차순으로 동적 부여하여, WebGL 내부 렌더링 우선순위를 강제로 고정시켰습니다.
  * 카드가 완전히 포개졌을 때 깊이 버퍼가 꺼져 있어 겹침 가림이 오작동하는 누수를 막기 위해, 카드 중심부에 카드 크기보다 가로/세로를 약 0.04 미세 축소한 아주 얇은 불투명 코어 메쉬(`depthWrite: true`, 흰색)를 샌드위치 구조로 심었습니다. 이로써 가장자리 유리의 영롱한 굴절은 훼손하지 않으면서도 물리적인 겹침 가림이 정확히 동작하게 구현했습니다.

## 15. Next.js 16 (Turbopack) & PostCSS 환경에서 @import 순서 엄격성 및 CSS 파싱 오류 해결
* **현상**: Next.js v16 + Turbopack 최적화 컴파일러 환경에서 `globals.css` 내부 최상단이 아닌 중간 영역에 웹 폰트(Pretendard 등) `@import` 구문을 작성하거나 TailwindCSS `@import` 지시어와 혼용 시, `Parsing CSS source code failed` 컴파일 에러가 발생하며 정적 빌드가 완전히 실패하는 현상이 발생했습니다.
* **원인**: PostCSS 스펙에 따르면 CSS 파일 내 모든 `@import` 선언은 `@charset` 혹은 `@layer`를 제외한 일반 CSS 규칙(예: `:root`, `body` 등)보다 반드시 앞서 정의되어야 합니다. 번들 과정에서 규칙 순서가 꼬이거나 다른 CSS 지시어 뒤로 밀릴 경우 문법 오류로 간주하여 컴파일이 불가능해집니다.
* **해결책**:
  * 글로벌 CSS 내에서 외부 CDN `@import` 문을 제거했습니다.
  * Next.js의 루트 레이아웃(`app/layout.tsx`) 내 `<head>` 영역에 직접 `<link rel="stylesheet">` 태그를 삽입해 브라우저 단에서 폰트 파일을 병렬 로드하도록 구조를 이원화했습니다.
  * 이를 통해 PostCSS 컴파일 파이프라인의 에러를 원천 차단하고 빌드 무결성을 확보했습니다.

## 16. 진짜 얇은 유리판(Glass Plate) 표현을 위한 앞면 텍스처 투명화 및 동적 Opaque Core 제어
* **현상**: 카드를 유리 재질로 설정했음에도 불구하고, 앞면 텍스처 배경이 불투명 흰색(`rgba(255, 255, 255, 0.95)`)으로 채워져 있고, 중심부의 불투명 코어(`depthCore`)가 항상 켜져 있어 카드의 투명 투과 효과가 전혀 나타나지 않고 불투명한 카드처럼 보였습니다.
* **해결책**:
  * 카드 앞면 텍스처(`TextureGenerator.ts`)의 배경을 `ctx.clearRect`로 투명하게 지워, 여백 영역이 맑은 유리 몸체를 그대로 드러내도록 수정했습니다.
  * `sideGlassMat`, `faceGlassMat`, `backGlassMat`의 물리적 속성(`roughness: 0.03`, `clearcoat: 1.0`, `clearcoatRoughness: 0.02`, `transmission: 0.99`)을 일치시켜 단일 유리판으로 느껴지도록 동기화했습니다. 유리의 인공적인 스카이 블루 틴트를 표현하기 위해 **텍스처 자체의 배경 영역에 20% 불투명도의 하늘색(`rgba(130, 227, 240, 0.2)`)을 주입하고, 옆면(`sideGlassMat`)의 재질 컬러를 `#82e3f0`로 설정**했습니다. 또한, Three.js의 물리적 투과(transmission) 구현을 위해 `transparent: false`로 셋팅하여 알파 폐기로 인한 스펙큘러 하이라이트 및 굴절 왜곡 훼손을 차단하고 영롱하게 푸른빛이 도는 세련된 유리판을 구현했습니다.
  * 카드의 내부 텍스트와 문양은 **실제 플레잉 카드의 표준 규격에 맞추어 스페이드와 클로버는 검은색(`#1A1A1A`), 하트와 다이아몬드는 빨간색(`#D32F2F`)으로 잉크 텍스처를 튜닝**하고, HUD 대시보드의 심볼 데이터도 이에 맞게 일치시켜 통일감을 부여했습니다. 또한, **글자와 테두리선 등 잉크 부분이 투명하게 날아가는 현상을 방지하기 위해 앞면 투과율 마스크인 `createCardFaceTransmissionMap`을 생성하여 `faceGlassMat`의 `transmissionMap`에 바인딩**했습니다. 잉크의 색상이 재질 자체의 하늘색 틴트 컬러와 혼합되어 어둡고 투명하게 뭉개지는 현상을 방지하기 위해 앞/뒷면 재질의 `color`를 `#ffffff`(순백색)로 설정하여 잉크 본연의 빨간색/검은색이 100% 불투명하고 또렷하게 인쇄되도록 완성했습니다. **더불어 3D 카드 스케일링과 회전 시 글자의 선명함을 극대화하고자 텍스처 캔버스 해상도를 2배(1024x1536)로 올리고 드로잉 요소를 비례 확대했으며, Three.js 텍스처의 비등방성 필터링(`anisotropy: 16`) 및 밉맵 연산(generateMipmaps: true)을 적용하고, 앞면 유리의 물리적 두께(`thickness: 0.005`)를 극소화하고 거칠기(`roughness: 0.08`)를 미세 상향해 굴절 일그러짐과 하이라이트 눈부심을 억제하여 최종적인 칼날 같은 선명도를 완수했습니다.**
  * 게임의 공정성을 위해 딜러의 카드 정보가 뒤에서 투과해 보이지 않도록 막는 `depthCore`를 항상 켜두는 대신, 카드의 공개 여부(`card.isHidden`)와 연동하여 동적으로 `visible` 속성을 제어했습니다. 이로써 비공개 상태에는 완벽한 은폐를 보장하고, 공개되는 즉시 맑은 유리판으로 변하게 하여 비주얼 퀄리티와 게임 규칙을 모두 만족시켰습니다.

## 17. 이중 데칼 샌드위치(Double Decal Sandwich) 아키텍처를 통한 영롱한 3D 굴절 유리와 잉크 가독성 양립
* **현상**: 카드 앞/뒷면 유리 틴트의 3D 물리 굴절 투과(`transmission: 0.99`) 연산 중 잉크(글자/문양) 레이어가 조명 및 프레넬 정반사, 그리고 깊이 소팅(`depthWrite: false`) 오류와 엮여 하얗게 가려지거나 뿌옇게 지워지는 가시성 문제가 계속되었습니다. 그렇다고 알파 블렌딩(`transparent: true`) 방식으로만 선회하면 3D 유리의 고급스러운 굴절 왜곡(refraction) 효과가 통째로 사라져 디자인이 밋밋해지는 딜레마가 있었습니다. 또한, 카드가 서로 겹쳤을 때 아래 카드가 완전히 비치거나 경계가 명확하지 않아 레이어의 깊이감(층위감)이 잘 표현되지 않았고, 카드 바깥쪽의 아날로그 검은색 테두리선이 투명 유리의 세련된 미니멀 감성을 다소 반감시켰습니다.
* **해결책**:
  - **이중 데칼 구조 개편**: 카드 본체 메쉬(`cardMesh`)의 6개 면 전체에는 옆면 유리 재질인 `sideGlassMat`을 일괄 적용해, 원래의 입체적이고 영롱하게 굴절 투과되는 3D 틴트 유리를 100% 복구했습니다. 그리고 그 위아래 표면에서 아주 미세한 여백(`0.002`)을 띄우고, 잉크 텍스처만 입힌 얇은 평면 데칼 메쉬(`PlaneGeometry`)를 얹어 일반 `MeshBasicMaterial`로 렌더링했습니다.
  - **앞면 검은색 테두리선 제거**: 앞면 텍스처 드로잉 중 `strokeRect` 테두리선 코드를 삭제하여 극도로 세련된 미니멀 프레임리스 유리 카드 디자인을 연출했습니다.
  - **배경 옅은 불투명화 (레이어 층위감 확보)**: 카드 몸체 유리(`sideGlassMat`)의 투과율(`transmission`)을 `0.99`에서 `0.85`로 하향 조정하고, 거칠기(`roughness`)를 `0.06`으로 헤이즈 조율하여 은은하게 뽀얀 연무 질감을 주었습니다. 또한 앞/뒷면 데칼 텍스처 배경에 `rgba(255, 255, 255, 0.12)`로 옅은 불투명 흰색을 주입했습니다. 이 조치들을 통해 카드가 겹쳤을 때 아래에 깔린 카드가 자연스럽고 은은하게 뿌옇게 가려지도록 유도하여 레이어의 깊이감을 극대화했습니다.
  - **쨍한 순수 원색 매핑**: spades/clubs는 `#000000`(순수 블랙), hearts/diamonds는 `#FF0000`(순수 레드)으로 교체하고 뒷면 써클 `#0055FF`(쨍한 파랑), 다이아몬드 및 'B' 로고 `#FFCC00`(쨍한 골드)을 매핑하여 가독성을 보장했습니다.

## 18. 유리 두께감 보존을 위한 계단식 적재 단차, 물리적 경사 적재 연출 및 호버 레이어 뚫림 방지
* **현상**: 카드 두께(`cardThickness: 0.12`)가 다소 두꺼워 둔한 느낌을 주었고, 딜링 시 카드가 수평으로 평평하게 겹쳐져 두께가 있는 실제 유리판이 테이블에 포개지는 듯한 입체적인 엇갈림 연출이 부족했습니다. 또한 마우스 호버 시 아래쪽에 깔린 카드가 Y축 위로 크게 솟구쳐 오르면서 위에 쌓인 유리 카드를 뚫고 올라오는 3D 그래픽 뚫림(Clipping) 현상이 발생했습니다.
* **해결책**:
  - **유리 두께 최적화**: `cardThickness` 변수를 `0.06`으로 절반 축소하여 날카롭고 세련된 유리판 두께를 연출했습니다.
  - **물리적 경사 적재 연출 (Y단차 + Z경사각)**: 오른쪽에 놓이는 카드가 왼쪽에 먼저 쌓인 유리판 위로 자연스럽게 올라타게 되므로, 첫 번째 카드는 바닥에 평평하게 눕히고(`targetRotZ = 0`, `targetY = 0.03`), 두 번째 카드부터는 왼쪽 가장자리가 올라타 기울어지도록 **일관된 Z축 음수 경사각인 `-0.07` 라디안(약 -4도)** 및 계단식 높이(`targetY = 0.03 + index * 0.035`)를 일괄 적용하여 진짜 물리적으로 포개진 입체 연출을 완수했습니다.
  - **호버 뚫림 방지 가드 (Top Card Lift 제어)**: `cardGroup.userData`에 인덱스 정보를 기록해두고, 호버 Raycast 루프 내에서 자신이 **가장 위에 쌓인 카드(Top Card)**인 경우에만 Y축 솟구침 들썩임(`0.25`)을 허용하고, 아래에 깔린 카드들은 솟구침을 극소화(`0.04`)하고 틸팅 흔들림만 받도록 제어하여 뚫림 현상을 방지했습니다. 이탈하여 복귀할 때도 엇갈렸던 원래의 Z축 경사각(`originalRotZ`)으로 회귀하도록 원복 리셋 공식을 튜닝했습니다.

## 19. Canvas/Konva 렌더링 컨텍스트 내 이미지 색상 필터링 한계 및 무손실 파일 변환 최적화
* **현상**: 단색 검은색 로고 이미지(`gongwon.png`)를 어두운 캔버스 노드 맵(`root` 노드 배경색 `#2C2C2C`)이나 사이드바 내에 띄울 때, 대비 확보를 위해 색상을 흰색으로 반전시켜야 했습니다. 그러나 HTML `<img>`와 달리, HTML5 Canvas API를 사용하는 `react-konva`(`KonvaImage`) 컨텍스트 내에서는 CSS `filter: invert(1)` 속성이 직접 적용되지 않아 반전 렌더링이 까다로웠습니다.
* **해결책**:
  * 브라우저의 런타임에 canvas 픽셀 데이터를 실시간으로 순회하며 인버트하거나 Konva 필터를 활성화하는 것은 성능(FPS) 저하와 렉을 유발할 수 있습니다.
  * 이를 해결하기 위해 Pillow 라이브러리를 활용한 Python 스크립트로 검은색 로고의 투명도(알파 채널)는 그대로 유지하면서 픽셀 색상만 순백색(255, 255, 255)으로 변환한 고품질의 **`gongwon_white.png`** 파일을 빌드 타임에 별도 물리 파일로 사전 생성해 두었습니다.
  * 이를 통해 런타임 연산 오버헤드와 캐싱 비용을 완전히 제로화하면서 어두운 영역에는 `gongwon_white.png`를, 밝은 영역에는 `gongwon.png`를 매칭하여 최상의 렌더링 시인성과 성능 무결성을 양립시켰습니다.

## 20. 3D 투명 유리와 양면 데칼의 뎁스 충돌 해소 및 단면 렌더링(Culling)을 활용한 비대칭 투과 연출
* **현상**: 3D 유리 카드 메쉬(`MeshPhysicalMaterial`, `transmission: 0.85`)의 앞/뒷면에 아주 얇은 데칼 평면 메쉬(`PlaneGeometry`)를 샌드위치 구조로 덧대어 텍스처를 렌더링할 때, 카드를 앞에서 바라볼 때 유리 몸체를 통과하여 저 뒤쪽의 테이블 배경이 보여야(관통) 함에도 불구하고 뒷면 데칼 메쉬가 뒤쪽 렌더링 버퍼를 완전히 블로킹하여 관통감이 훼손되는 문제가 있었습니다.
* **원인**: 뒷면 데칼 머티리얼(`backDecalMat`)의 `depthWrite` 속성이 `true`로 설정되어 있어, 비록 앞쪽에서 바라볼 때는 Backface Culling(`FrontSide` 적용)으로 인해 뒷면 데칼의 픽셀이 그려지지 않더라도, 뎁스 버퍼에는 이 영역의 깊이 값이 불투명하게 기록되었습니다. 이로 인해 데칼 뒤에 그려져야 할 배경 객체들이 뎁스 테스트에 탈락해 렌더링에서 통째로 누락되었습니다.
* **해결책**:
  * 앞면 데칼(`faceDecalMat`)과 뒷면 데칼(`backDecalMat`)의 `depthWrite` 속성을 모두 **`false`**로 설정하여 뎁스 버퍼 기록을 차단했습니다.
  * 단면 렌더링의 기본 Culling 속성에 의해, 앞쪽에서 카드를 볼 때는 뒷면 데칼이 완전히 무시되어 투명한 3D 유리를 통해 뒷배경(테이블 등)이 정상 관통하여 비치게 유도하였습니다.
  * 카드를 뒤집어서 뒤쪽에서 바라볼 때는 불투명한 차콜 그레이(`#1A1A1A`) 텍스처 배경이 뎁스 버퍼 기록 없이도 그려질 픽셀들을 완벽히 불투명하게 차단해 주므로, 뒤쪽 유리가 전혀 비치지 않는 완벽한 물리적 은폐를 보장하여 비대칭적이고 정교한 3D 뎁스 관통 룩앤필을 구현해 냈습니다.

## 21. 상태 변화(isHidden)에 따른 3D 오브젝트 재질(Material) 동적 스위칭
* **현상**: 바닥에 뒤집혀 놓인 비공개 카드(isHidden 상태)의 옆면이 투명/흰색 유리 재질로 도드라져, 검은색 뒷면 텍스처 및 검은색 덱 더미와 이질감이 생기는 현상이 있었습니다.
* **해결책**:
  * 카드의 투명성을 살려둔 상태에서 단순히 불투명 샌드위치 코어(`depthCore`)만 가시성을 켜고 끄는 방식으로는 옆면 유리의 틴트 광택과 굴절을 가릴 수 없었습니다.
  * 이를 해결하기 위해 `CardBuilder.ts` 내에 카드 본체 유리 메쉬에 `'cardBody'` 라는 이름을 명명하고, 비공개 상태일 때는 불투명한 차콜 블랙 표준 재질(`opaqueBackMat`, `#1A1A1A`)을 할당하도록 하였습니다.
  * `CardAnimator.ts` 내의 실시간 애니메이션 루프 영역에서 `card.isHidden` 플래그의 갱신에 발맞추어 `cardBody.material`을 `opaqueBackMat`(비공개 시)과 `sideGlassMat`(공개 시) 사이에서 동적으로 스위칭하도록 구현하여, 비공개 시에는 옆면까지 검은색 더미로 감추고 카드가 뒤집히며 공개되는 즉시 다시 영롱한 유리 재질로 원활하게 변환되는 고도화된 3D 게임 연출을 완수했습니다.

## 22. 3D 리소스 비동기 로딩과 페이지 이동 트랜지션 애니메이션 싱크 동기화 (Transition Hold & Release)
* **현상**: 상세 페이지(`/works/blackjack`) 진입 시, 3D WebGL(HDR 및 씬 설정) 리소스가 로딩 중인데도 트랜지션 덮개가 즉시 걷혀 빈 검은 화면이 일시 노출되는 현상이 계속되었습니다.
* **원인**:
  1. 리액트의 `useState` 상태 업데이트 비동기 특성으로 인해, `page.tsx` 마운트 시 `holdTransition()`을 호출하여 `isTransitionHeld(true)` 상태 갱신을 큐에 쌓더라도, 동일 마운트 이펙트 실행 틱 내의 `layout.tsx` 이펙트에서는 상태 초기값(`false`)을 읽어 트랜지션이 조기 구동되는 **마운트 레이스 컨디션**이 원인이었습니다.
  2. 리소스가 실제로 100% 완료된 시점(오버레이 페이드아웃 시작 시점)과, 오버레이 페이드아웃 애니메이션 완료(0.8초 지연) 후의 콜백 시점이 혼재되어 트랜지션 걷히기가 무의미하게 추가 지연되었습니다.
* **해결책**:
  * **State-Sync Ref 패턴 도입**: `TransitionContext` 내부에 반응형 `isTransitionHeld` 상태와 동기식 `isTransitionHeldRef` 참조 변수를 병렬 제공했습니다. `holdTransition()`, `releaseTransition()`, `resetTransition()` 함수 내에서 상태와 Ref 값을 동기식으로 일치시켜 마운트 틱 타이밍 이슈를 완벽히 해결했습니다.
  * **동기식 이중 가드**: `layout.tsx` 마운트 즉시 `isTransitionHeldRef.current` 값을 체크해 동기적으로 트랜지션 재생을 스킵(return)하고, 나중에 로드 완료 후 `isTransitionHeld` 상태 전이 시에 `useEffect` 의존성 트리거에 의해 비로소 `playInDetailTransition()`이 구동되도록 견고하게 가드를 쳤습니다.
  * **HDR 로드 콜백 분리 및 0.5초 딜레이 적용 (`onLoad`)**: `.hdr` 파일 다운로드가 끝난 시점 (`EnvironmentLoader`의 onLoad 시점) 즉시 동작하지 않고, 3D 씬 안정화 및 여유 있는 전환을 위해 0.5초(500ms)의 `setTimeout` 대기 시간을 부여한 뒤 `releaseTransition()`을 트리거하여 덮개가 열리기 시작하도록 튜닝했습니다.


## 23. 3D 카드 딜링 애니메이션의 물리적 선후 관계 제어 및 지연 상태(Delayed State)를 활용한 UI/UX 싱크 최적화

* **현상**:
  1. 카드 딜링 시 GSAP의 `ease: 'back.out'` 속성으로 인해 카드들이 바닥에 꽂힐 때 불필요하게 출렁거리고 흔들리는 현상이 발생해 사용자 몰입감을 저해했습니다.
  2. 덱에서 카드를 가져올 때 카드가 날아가는 도중에 뒤집어지는 등의 연출이 일어나 실제 물리 법칙에 어긋나 보였으며, 카드 생성 위치가 덱의 최상단보다 낮게 생성되어 16번째 덱 카드와 겹쳐서 미끄러져 나오는 어색함이 있었습니다.
  3. 카드가 비행 중이거나 뒤집어지는 와중에 화면 상단의 알림 메시지나 하단의 딜러/플레이어 점수, 승패 판정이 즉시 갱신되어, 시각적 정보와 실제 애니메이션 사이에 심각한 단절(Sync 문제)이 생겼습니다.
* **원인**:
  1. 연출의 역동성을 위한 탄성 이징(Elastic, Back)이 얇고 단단한 유리 블록 메타포에는 과하게 흔들리는 어색함을 초래했습니다.
  2. 스폰 위치(기존 Y=0.858)와 덱 적층 최상단 Y 높이(16번째 카드 중심 0.858 + 두께 0.06/2 = 표면 높이 0.888)의 기하학적 오차로 인해, 17번째 신규 드로우 카드가 덱 맨 윗장와 겹쳐서 자가분열되는 형태를 띠었습니다. 또한, 3D 틴트 유리의 투과성(`transmission`) 때문에 뒤집혀 비행하더라도 바닥을 향한 앞면 잉크 텍스처가 위로 고스란히 비쳐 보여서 시각적으로 이동 중에 이미 앞면이 다 뒤집힌 것처럼 인지되었습니다.
  3. **결정적인 비행 중 회전 원인**: `HoverInteraction.ts` 파일 내에서 매 프레임(`requestAnimationFrame`)마다 마우스 호버 이탈 카드를 원래 각도로 복귀시키는 LERP 보간 루프(`THREE.MathUtils.lerp(group.rotation.x, origRotX, 0.1)`)가 돌고 있었습니다. 이 루프가 날아가는 카드에도 작동하여, 비행 중인 카드의 `rotation.x`를 목적지 타겟 각도인 `-Math.PI / 2`로 실시간 강제 보간해버려 비행 도중에 홱 뒤집어지는 시각적 버그가 유발되었습니다.
  4. React Reducer 기반의 게임 엔진이 사용자의 버튼 클릭(Stand, Hit 등) 즉시 모든 카드 배치와 승패, 최종 결과를 한 틱 만에 동기적으로 변경하여 반환했고, React UI 레이어는 이를 딜레이 없이 실시간 렌더링했기 때문입니다.
* **해결책**:
  - **오버슈트 제거**: GSAP 이동 트윈의 이징을 `power2.out`으로 일괄 통일하여 흔들림 없이 정확한 타겟 지점으로 정밀 착지되도록 보정했습니다.
  - **선(先) 이동 후(後) 뒤집기 체이닝 & 덱 높이 매칭**: 스폰 높이를 덱 최상단 카드 위에 포개지는 높이인 **`Y=0.92`**로 정밀 동기화하여 기하학적 겹침을 원천 차단했습니다. 또한 비행 중 앞면이 비쳐 보이는 현상을 해소하기 위해 **이동 중에는 모든 카드의 재질을 불투명 차콜 그레이(`opaqueBackMat`)로 강제하고 내부 불투명 코어를 켜둠으로써 앞면 투과를 완전 봉쇄**했습니다. 카드가 바닥에 도착한 후에만 뒤집기 트윈을 구동하고, `onStart` 콜백에서 비로소 투명 유리판 재질(`sideGlassMat`)과 코어 비활성화를 수행하여 물리적으로 정확한 아날로그 뒤집기 경험을 제공했습니다.
  - **매 프레임 LERP 간섭 차단 가드 도입**: `CardAnimator.ts` 에서 애니메이션이 활성화될 때 `cardGroup.userData.isAnimating = true` 가드 플래그를 할당하고 완료 시 해제합니다. 이 플래그가 켜진 카드는 `HoverInteraction.ts` 의 마우스 호버 및 이탈 복구 LERP 루프 연산에서 **완전히 스킵(Skip)**되도록 상호 가드를 심어 비행 중에 그 어떤 미세 회전 간섭도 발생하지 않도록 완전 무결하게 격리 차단했습니다.
  - **지연 상태(Delayed State) 아키텍처 도입**: `CardAnimator.ts` 내의 모든 딜링 및 뒤집기 트윈의 수명주기를 `Promise`로 감싸고, `BlackjackCanvas`에서 `Promise.all`을 수행하여 전체 카드 비행 및 플립 완료 시점(`onAnimationComplete`)을 트랙킹했습니다. `page.tsx` 내부에서는 실제 게임 상태를 임시 백업해두는 로컬 지연 상태(`displayed...`)와 `isAnimating` 플래그를 도입해, 애니메이션이 돌고 있는 동안에는 HUD 정보를 이전 상태로 고정하고 Hit/Stand 버튼 등을 강제 비활성화(`disabled={isAnimating}`)했습니다. 모든 애니메이션이 최종 완료되면 실제 상태값을 최종 커밋(Commit)하게 하여 시각적 애니메이션 완료 타이밍과 UI 판정 노출 시점을 완벽하게 일치시켰습니다.

## 24. 3D WebGL Blackjack에서 딜러 턴 드로우의 비동기 순차화 및 React UI의 리액티브 루프 동기화
* **현상**: 플레이어가 Stand 또는 Double Down을 선언했을 때 딜러 턴이 즉시 동기식(while 루프)으로 끝까지 전개되어 모든 카드가 한꺼번에 스폰되거나, 혹은 딜러 드로우가 너무 빨리 연달아 일어나 비주얼 애니메이션이 끝나기도 전에 다음 드로우 연산 및 점수 판정이 다발적으로 꼬이는 현상이 발생했습니다.
* **원인**: Reducer 엔진 내에서 딜러 플레이 턴을 한 번의 동기식 액션으로 처리하여 반환했기 때문이며, React의 상태 변경 라이프사이클과 Three.js 씬 렌더링 프레임 간의 틱 차이로 인해 `isAnimating` 상태가 `true`로 셋팅되기도 전에 리액티브 루프가 여러 차례 반복 호출되는 레이스 컨디션이 유발되었습니다.
* **해결책**:
  1. Reducer 내에서 딜러 차례를 `DEALER_DRAW_CARD`와 `RESOLVE_GAME`으로 점진 분할하여, 한 번에 한 장씩만 카드를 가져오거나 정산하도록 설계했습니다.
  2. `page.tsx` 단에 `stage === 'DEALER_TURN' && !isAnimating` 상태를 실시간 감지하는 `useEffect` 루프를 배치하고, 각 단계마다 0.9초의 아날로그 간격을 두어 다음 단계(17점 미만 시 추가 드로우, 이상 시 판정)를 결정하게 유도했습니다.
  3. 비동기식 렌더링 갭에 의해 UI 상태가 이중 드로우를 트리거하지 못하도록, 사용자 조작(`Deal`, `Hit` 등) 및 딜러 자동 드로우 타이머 내에서 액션 함수를 실행하기 직전에 동기적으로 `setIsAnimating(true)`를 선제적으로 걸어 락(Lock)을 확보하는 방식을 정립했습니다.

## 25. Three.js 내의 3D 객체 파쇄(Shatter) 애니메이션 및 UV 맵핑을 통한 그래픽 디테일 유지
* **현상**: 버스트 시 유리가 깨지는 3D 파편 연출을 단순히 지오메트리를 흩뿌리는 방식으로 구현하면, 파편들이 단색 틴트 유리로만 렌더링되어 기존 카드 본래의 숫자 및 문양(페이스 텍스처) 인쇄면과의 연속성이 사라지는 이질감이 생겼습니다.
* **해결책**:
  - 격자(Grid) 배치 후 중심점들을 무작위로 흔든(Jitter) 40개의 삼각형 파편 조각들을 `ShapeGeometry`를 통해 생성했습니다.
  - 각 파편 정점들의 로컬 좌표를 기반으로 카드의 크기 비례 UV 좌표(`u = (x + W/2) / W`, `v = (y + H/2) / H`)를 역산해 `uv` 속성을 직접 주입했습니다.
  - 파편의 머티리얼 텍스처(`map`)에 원본 카드의 페이스 텍스처를 바인딩하여, 카드가 흩어질 때 숫자나 모양의 인쇄면이 리얼하게 조각나며 비산되는 연출을 확보했습니다.

## 26. Web Audio API 기반의 하이패스 노이즈 및 다중 피치 공명 결합을 통한 실시간 유리 파쇄음 합성
* **현상**: 유리 깨짐 연출을 실감나게 표현하기 위해 외부 `.mp3` / `.wav` 파일을 수급할 경우 네트워크 트래픽 및 오디오 버퍼 로딩 지연으로 인해 연출 시점과 사운드가 맞지 않는 싱크 어긋남 현상이 생길 수 있습니다.
* **해결책**:
  - `SoundManager.ts` 내에 `playShatter()` 메서드를 정의해 100% 코드로 실시간 사운드를 합성했습니다.
  - 1.5kHz ~ 5.2kHz 대역의 고주파 오실레이터 5개에 각각 미세 피치 흔들림(Jitter)과 개별 볼륨 엔벨로프(어택 직후 빠른 지수적 댐핑)를 부여해 파편들의 청량한 금속성 울림을 연출했습니다.
  - 이와 동시에 highpass 필터(2500Hz)를 거친 화이트 노이즈 버퍼를 믹싱하여 유리창이 순간적으로 박살 날 때 발생하는 둔탁하고 거친 크래킹 소음을 함께 합성하여 완벽한 유리 깨짐 입체음을 구성해 냈습니다.

## 27. 3D WebGL Blackjack 공간 왜곡 보정, 특정 각도 흐려짐 개선 및 정산 상태 동기화
* **현상**:
  1. 3D 뷰에서 디스플레이 패널(점수판)이 카메라 각도에 따라 간헐적으로 깜빡이거나 텍스처 배후와 겹치며 Z-Fighting 현상이 일어났고, 이전 점수가 캔버스 잉크 잔상으로 흐릿하게 남아 보였습니다. 또한, 마우스를 회전시켜 3D 평면을 비스듬하게 눕혀(Grazing Angle) 바라볼 때 텍스트(점수판, 버튼) 및 가이드라인이 흐리멍텅하게 뭉개지거나, 먼 쪽의 딜러 영역이 지나치게 탁하고 검게 흐려지는 화질 저하가 있었습니다.
  2. 3D 공간 상에서 힛(HIT)/스타트(START) 버튼과 스탠드(STAND) 버튼을 동일 선상에 두었음에도 Perspective Camera의 원근법에 의해 비대칭적으로 왜곡되어 겉보기 간격이 달라 보였으며, 화면 이동 범위(OrbitControls) 제한이 없어 사용자가 바닥 아래로 카메라를 처박거나 월드 경계 밖으로 이탈하는 시각적 붕괴가 생겼습니다.
  3. 승패 정산 시 바닥 조명 효과의 시인성이 낮았고, 이전 판에 깨진 유리 카드 파편(`shatteredCards`)이 새 게임 시작 시에도 제때 소거되지 않고 남아 겹치는 시각적 잡음이 존재했습니다. 특히 플레이어가 버스트(21점 초과)되었을 때, 딜러 필드가 아닌 **플레이어 필드가 오작동으로 연두색 발광**하는 레이스 컨디션 버그가 발생했습니다.
* **해결책**:
  - **Z-Fighting, 누운 각도 흐려짐 및 원경 흐려짐 전방위 개선**: 
    - 3D 점수판 메쉬에 `polygonOffset` 속성을 부여하고 Y 높이 오프셋을 `0.02`로 조정하여 뎁스 겹침을 방지했습니다. 디스플레이 패널의 `frustumCulled`를 `false`로 지정하여 렌더링 누락을 막았으며, 점수 갱신 직전 Canvas Context를 `clearRect(0, 0, w, h)`로 소거하여 잉크 잔상을 제거했습니다.
    - 비스듬한 시야각에서 Three.js가 밉맵을 뭉개는 문제를 해결하기 위해 점수판/버튼의 `CanvasTexture` 설정에 `generateMipmaps = false`, `minFilter = THREE.LinearFilter`를 주입하여 쨍한 가독성을 완수했습니다.
    - 투명도 깊이 판정이 엇갈리며 블렌딩이 흐려지던 문제를 해결하고자 바닥판(`renderOrder = 1`), 가이드라인(`renderOrder = 2`), 점수판(`renderOrder = 3`), 버튼(`renderOrder = 4`) 순으로 명시적 레이어 렌더링 순서를 부여했습니다.
    - 씬 전체의 안개(`FogExp2`) 밀도를 `0.06`에서 **`0.015`**로 약화하여 원경의 먼 가이드라인이 검은 안개에 지워지던 현상을 해소했습니다.
  - **원근 왜곡 정렬 및 카메라 제한**: 투영 왜곡 보정값을 수치화(1.62)하여 Z축 대칭을 구현하였으며, 스탠드 버튼의 아랫날과 가이드 라인 끝 단을 정밀하게 일치시켰습니다. `OrbitControls`의 `controls.enablePan = false`를 부여해 평행 이동을 차단하고 `maxDistance = 9.8`로 설정하여 월드 이탈 및 바닥 하단 돌파를 방지했습니다.
  - **시인성 강화, 버스트 발광 락 및 상태 공급 이원화**: 
    - 승패 바닥판의 기본 색상을 연두색(`#82ff93`, opacity 0.22)으로 튜닝하여 정산 피드백 시인성을 대폭 끌어올렸으며, 새 라운드 시작 클릭 직후 씬 내 파편들을 즉시 클리어하도록 정비했습니다.
    - 버스트 시 플레이어 바닥이 오발광하던 문제는 실시간 stage와 UI 갱신을 늦추는 지연 stage의 타이밍 갭이 원인이었습니다. `page.tsx`에서 지연 stage(`displayedStage`)를 `stage` prop으로 넘겨주어 `displayedWinner`와 갱신 템포를 동기화하고, 즉각적인 물리 비행 및 파편 깨뜨림 연출은 실시간 스테이지 상태를 담은 **`rawStage` prop**을 신규 이원화 공급하여 해결했습니다. 또한, 플레이어 버스트 직후 딜러 카드가 다 뒤집힐 때까지 딜러 스코어가 갱신되지 않아 생기던 답답함을 해결하기 위해, UI 동기화 `useEffect` 조건절에 버스트 정산 예외 처리를 탑재하여 카드 공개 애니메이션이 시작되는 첫 움직임과 거의 동시에 딜러의 최종 점수가 전광판에 즉각 업데이트되도록 보정했습니다.

## 28. 새 게임 시작 시 이전 판 잔상 즉각 소거 및 00 갱신 후 카드 배분 이분화 연출
* **현상**:
  - 새 게임을 시작하기 위해 START를 누르는 즉시 카드가 덱에서 날아오기 시작하는데, 이 딜링이 이루어지는 도중에도 이전 게임의 연두색 바닥 승리 조명 및 이전 점수가 점수판에 그대로 남아있어 새 게임으로의 아날로그 트랜지션이 지저분하게 연출되었습니다.
* **해결책**:
  - **리듀서 및 액션 분할**: `RESET_GAME` 액션에서는 오직 덱 셔플, 핸드 비우기, `winner = null`, `stage = 'DEALING'` 설정만을 하도록 단순화하고, 실제 카드 4장 분배는 `DEAL_INITIAL_CARDS` 라는 별도 신규 액션으로 분리 이관했습니다.
  - **점수 00 렌더링 예외 처리**: `Clocks3D.ts` 의 점수판 갱신 시 `score >= 0` 검사로 정정하여, 딜링 진행 중(stage = 'DEALING')일 때 0점을 `'00'` LED 숫자로 점수판에 선명히 투사하게 보정했습니다.
  - **시간차 트랜지션 조율**: START 클릭 시 즉시 `startNewGame()` 리듀서를 작동하고 `displayed...` 지연 상태값들(점수 0, 위너 null, stage 'DEALING')을 동기적으로 리셋하여 딜링 시작 전 바닥 조명이 즉시 꺼지고 점수판이 `00`으로 갱신되도록 제어했습니다. 그 후 500ms의 아날로그 대기 딜레이를 주어 유리 파편 소거와 조명 페이드아웃이 다 가라앉은 것을 보여준 뒤, `dealInitialCards()`를 디스패치해 덱에서 카드가 비행하도록 유도함으로써 아주 매끄럽고 물리적으로 완벽하게 정돈된 아날로그 게임 시작 연출을 환수했습니다.

## 29. 딜러 버스트 연출 시퀀스 개선 (선 UI 반영 후 깨짐 애니메이션)
* **현상**:
  - 딜러가 21점을 초과(버스트)할 때, 카드가 날아와 뒤집히자마자 카드가 쨍그랑 깨지고, 깨지는 애니메이션(약 1.2초)이 다 끝난 후에야 `isAnimating` 락이 풀려 승패 최종 결과(플레이어 승리 연두색 바닥 조명, 색수차 프리즘 효과 등)와 딜러 최종 점수가 LED 점수판에 반영되었습니다. 이로 인해 유리가 깨질 때 점수와 승패 결과가 일치하지 않아 연출 상 답답함과 단절감이 존재했습니다.
* **해결책**:
  - **선 정산 및 UI 동기화 가드 해제**: 딜러 버스트 카드가 뒤집히는 애니메이션 완료 즉시, `BlackjackCanvas`에서 `onDealerBust` 콜백을 트리거하여 즉각 `resolveGame()`이 작동해 게임 정산 상태(`stage = 'RESOLVED'`, `winner = 'player'`)로 진입하도록 조치했습니다. 또한 `page.tsx` 내부의 UI 상태 동기화 `useEffect`에 `isDealerBustResolved` 조건을 추가해, 3D 애니메이션 락(`isAnimating: true`) 상황 하에서도 점수와 승리 필드 효과가 즉시 화면에 커밋되도록 보정했습니다.
  - **딜레이 제거를 통한 즉각 연쇄**: 정산 콜백 트리거 후 지연 시간을 제거하고, 즉각적으로 `shatterCard`를 작동시켜 딜러 버스트 UI 갱신(점수판, 바닥 조명)과 유리가 깨지는 물리 연출이 거의 동시에 연쇄적으로 맞물려 실행되도록 시퀀스 템포를 긴장감 있게 튜닝했습니다.

## 30. 플레이어 블랙잭(내추럴 21점) 달성 시 쨍한 노란빛 바닥 조명 동적 트랜지션
* **현상**:
  - 플레이어가 처음 두 장으로 21점(내추럴 블랙잭)을 달성하여 승리하거나 무승부를 거두어도, 승리 바닥 조명 효과가 일반 승리와 동일하게 싱그러운 연두색(`color: '#82ff93'`)으로 켜져, 게임 내 최고의 조합인 '블랙잭'에 대한 시각적인 피드백과 보상 연출이 밋밋한 아쉬움이 있었습니다.
* **해결책**:
  - **내추럴 블랙잭 판별 및 쨍한 노란색 적용**: `BlackjackCanvas.tsx` 내부의 `Effect 3` (승패 비주얼 연출) `useEffect`의 종속성 배열에 `playerScore`를 추가하고, 3장 이상 카드를 받은 21점 승리가 아닌 오직 처음 2장으로 21점을 달성한 상황(`playerScore === 21 && playerHand.length === 2`)만 내추럴 블랙잭으로 감지하도록 제한했습니다.
  - 타겟 색상을 기존 황금빛보다 더 쨍한 순수 노란색인 `#ffff00`으로 튜닝하고, GSAP을 활용해 3D 바닥 조명 머티리얼의 색상 객체(`material.color`)의 r, g, b를 목표값으로 0.6초간 부드럽게 트윈시켰습니다.
  - 또한, 게임의 대기 및 진행 단계(`stage !== 'RESOLVED'`)로 복귀하거나 패배할 때도 기본 연두색(`color: '#82ff93'`)으로 부드럽게 색상을 원복함으로써, 3D 리소스 생성/소거 비용 없이 기존 머티리얼의 색상 트랜지션만으로 블랙잭에 대한 특별하고 화려한 아날로그 비주얼 보상을 영롱하게 완성했습니다.

## 31. 비동기 딜레이 정산 갭 사이의 연타 방어 및 리듀서 가드 확보 (플레이어 버스트 시 오발광 버그 차단)
* **현상**:
  - 플레이어가 버스트(21점 초과)되는 카드를 분배받은 후, 카드가 완전히 날아가 뒤집혀 안착하는 동안은 `isAnimating`이 `true`로 설정되어 조작 버튼이 차단되었습니다. 하지만 안착이 완료되는 찰나에 `isAnimating`은 `false`로 풀리고 실제 버스트 정산(`resolveBust()`) 디스패치는 **600ms 뒤**에 실행되도록 비동기 설계되어 있었습니다.
  - 이 600ms의 짧은 타이밍 갭 동안 UI 버튼이 다시 활성화되었으며, 사용자가 이 찰나에 빠르게 `Stand`를 수동으로 연타하여 클릭하는 경우, `stage`가 `DEALER_TURN`으로 넘어가 버스트 정산이 취소되고 딜러 턴이 작동해 결국 정산 시 플레이어 버스트임에도 승리하여 플레이어 필드에 연두색 승리 조명이 오발광하는 치명적인 게임 로직 붕괴 버그가 발생했습니다.
* **해결책**:
  - **1) UI 레벨 조작 락 조건 강화**: `page.tsx`에서 `BlackjackCanvas`로 전달하는 `isAnimating` prop의 조건식을 `isAnimating || (stage === 'PLAYER_TURN' && playerScore > 21)` 로 개선하였습니다. 이로써 버스트 카드가 안착하여 `isAnimating`이 잠시 `false`가 되더라도, 버스트 정산이 완료되기 전(`stage === 'PLAYER_TURN' && playerScore > 21`) 600ms 동안에도 조작 락을 강제로 유지시켰습니다.
  - **2) 엔진 레벨 방어 가드 주입**: `BlackjackEngine.ts` 내부의 `HIT` 및 `STAND` 리듀서 액션 내부 최상단에 `if (calculateHandScore(state.playerHand) > 21) return state;` 가드 절을 추가했습니다. 이로써 혹시 모를 비동기 이벤트 갭에서도 이미 버스트된 핸드에 대해서는 그 어떠한 조작 액션도 엔진 단에서 무시하도록 이중 안전망을 보장했습니다.





